version: null
meta:
  environment: python
  toolchain:
    name: pip
    version: "3.10"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  persistenceMount: /data
  containerPort: 8501
  command: streamlit run app/app.py
